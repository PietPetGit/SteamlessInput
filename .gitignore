# ---- Secrets - never commit, even if accidentally placed in the tree ----
Github_token.txt
Github_Token.txt
*.pem
*.key

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/

# ---- Build / PyInstaller output (release binaries come from CI, not the repo) ----
build/
dist/

# ---- CI release-packaging artifacts ----
SteamlessInput-*-windows.zip
SteamlessInput-*-linux.tar.gz
windows/lockscreen-keyboard/*.exe

# ---- Runtime / user state (regenerated by the app at runtime) ----
settings.json
triton_probe_*.txt

# ---- Release docs / task prompts kept outside the repo ----
release.txt
Push_Github.md
OLD VERSION/
done.txt
fix.txt
resume*.txt
.local-archive/

# ---- Reference-only working copies (NOT shipped: AGPL-licensed upstream;
# ----  we consume its public HTTP API only - see THIRD_PARTY_NOTICES.md) ----
steaminputdb.com-main/
steaminputdb.com-main.zip
# Triton HID protocol research repo, consulted while writing the report
# parser; reference only.
sc2-research-main/
sc2-research-main.zip
# The Ramonchi_5 viewer DIRECTORY is deliberately bundled (see
# THIRD_PARTY_NOTICES.md, MIT) - only its redundant download zip stays out.
Steam-Controller-Gamepad-Viewer-by-Ramonchi_5-main.zip

# ---- Local working notes / imported reference scripts (not part of the app) ----
options_settings.txt
sleep_manager_text.txt
SleepManager.bat

# ---- Editor / OS / agent scratch ----
AGENTS.md
CLAUDE.md
*.tmp
*.log
.DS_Store
node_modules/
.idea/
.vscode/
.codex/
.codex-local/
.claude/
claude-resume/
